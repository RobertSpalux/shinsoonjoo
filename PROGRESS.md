# PROGRESS — 신순주의 선한 금융 (프로젝트 진행 상황)

> 이 문서는 **Claude 웹 프로젝트의 맥락 앵커**다. 새 대화창에서도 이 문서 + 최신 소스 zip을 올리면 전체 맥락이 복구된다.
> 함께 프로젝트 지식에 올려둘 기준 문서: `CLAUDE.md`(헌법), `DESIGN-SPEC.md`(디자인 토큰), `CONTENT-STRATEGY.md`(콘텐츠·매출 전략).
> 최종 갱신: 2026-07-13 저녁 마감 (M4-1·M4-2 /admin 상록수 탭+허브 내부링크 + 상담 일화뱅크 + 버그 2건)
> **다음 세션 우선순위: ① 화 06:00 뉴스 크론 첫 실전 검증(O8 경어체·O9 게이트·O10 신규 소스 2종) ② 화 07:00 상록수 첫 자동 실행 검증(N6 CTA 마커·M4-2 허브링크 🔗·일화 🧾·verify_claims) ③ 허브글 검증 7건 실검수로 🔴 해제(운영).**

---

## 북극성 (최상위 목적 — 절대 잊지 말 것)
**모든 채널(웹·네이버블로그·블로그스팟·인스타/스레드)을 마케팅 자동화(OSMU)로 굴려 → 브랜드 신뢰 구축 → 리드(DB) 생성 → 상담 → 보험 리모델링 매출로 잇는 단일 수익 깔때기.**
- 수익 극대화가 목표. 모든 판단은 **'유입→관계→리드→매출' 사슬 기여도 + 자동화(사람 개입 최소)**로 검증한다.
- 콘텐츠는 하나의 **닫힌 엔진**으로 설계: 수집 → 검증(팩트게이트) → 생성 → 검수(자동/사람) → 배포 → 유입·측정 → (피드백)다시 수집. 개별 패치가 아니라 엔진 전체를 개선한다.

## 브랜드 포지셔닝 (핵심 — 카피·디자인의 뿌리)
- **표층(HOOK): "관계가 아니라, 분석으로."** 지인에게 맡긴 보험 → 담당자가 소리 없이 바뀜 → 열어보면 중복·과설계·부족한 보장 반복 → 신순주를 찾아옴. 그 패턴을 데이터로 읽으려 **직접 리모델링 분석 시스템 개발**(최대 차별점).
- **심층(SOUL):** "가장 선한 금융의 통로." (히어로에서 성경 인용문은 내리고 태그라인만 유지 — 아래 완료 참고)
- **전국구.** 지역명 배제, 비대면 전제. 소속·주소는 푸터만. (예외: 채용 페이지는 천안 유지 — 오프라인 지사 대면 코칭이 실제 천안)
- 컴플라이언스: 특정사 비방·수익률보장·과장·"무조건 해지" 금지. "점검·재설계" 프레임. 감정어 없이 담담하게.

## 디자인 방향 (확정)
- 라이트/에디토리얼. 웜 아이보리 배경 + 웜 차콜 텍스트 + 세리프 헤드라인(Noto Serif KR)/Pretendard 본문.
  - ⚠️ **웹은 세리프 헤드라인, 카드뉴스는 산세리프(Pretendard 800)로 확정** — 카드는 매체 특성상 굵은 산세리프가 시선 잡기에 유리(2026 금융 카드 1등 방식). 세리프는 웹 전용.
- **딥 그린 `#1b3a30`(확신 액센트) — 대비 밴드로만.** 크림↔딥그린 리듬. 전체 도배 금지.
- 골드는 얇은 선·작은 마크 "속삭임"만(딥그린 위 헤어라인). 면 채움·숫자색·헤딩 단어색 금지.
- **이미지: 인물 스톡·질감 레이어 모두 안 씀(확정).** 스톡 인물샷은 "관계가 아니라 분석" 브랜드와 충돌. 풍성함은 여백·타이포·딥그린 대비에서. 빈 공간은 이미지 아닌 "증거(숫자)"로 채움.
- ⚠️ 경력 숫자는 무조건 `getCareer()` 동적값(하드코딩 금지 — 불일치 사고 있었음).

## 확정된 사업 구조 (중요 — 2026-07-12 확정)
웹의 목표 = 리드 수집이 아니라 **상담 성사**.
상담 후: 설계사 권한으로 보험사 프로그램에서 전 계약 조회 → 로컬 분석 프로그램으로 리모델링
→ 유지/해지/신규 처방 → 매출.
- ⚠️ 주민번호·계약 실데이터는 웹이 절대 만지지 않음. 조회·분석은 오프라인/로컬 전용.
- 분석 프로그램은 담보 단위 합산 + **"잔여 공백 경고"**(자기 제안의 빈 곳도 보고) = 최대 신뢰 자산.

---

## ✅ 완료 (누적)
### 홈·기사·디자인 시스템 (이전 세션들)
- CLAUDE.md 헌법 / DESIGN-SPEC + globals.css(웜차콜·골드 격하·세리프·focus-visible)
- 히어로("당신도 모르게, 새고 있는 보험") / 홈 전파(Header·Footer·TrustSection·Timeline·ClientStories·ConsultationForm)
- 기사 페이지(세리프 h2·토큰·목차·핵심3줄·블록인용) / 딥그린 팔레트 도입(통계밴드·주CTA·헤딩·푸터)
- brand.ts 연락처·소셜(전화·주소·인스타 @goodfinance_sj·카카오 채널 pf.kakao.com/_xoxdBwX)

### 이번 세션 (2026-07-10)
- **[Task 1] 천안 전면 제거 + 6개 카테고리 반영**
  - 6 카테고리 확정: `보험료 절약·꿀팁` / `보험금 청구·보상` / `실손·보장성 가이드` / `연금·노후·세테크` / `보험 리모델링` / `금융·경제 뉴스`. (판례·분쟁은 나중 반자동, 자동 enum 제외)
  - factory-prompt enum·articles.ts·collect-and-generate 소스힌트 통일. DB 마이그레이션 완료(기존 4행 → `금융·경제 뉴스`).
  - 전국구: `LocalBusiness/InsuranceAgency` 스키마 → `Organization`(지역 없음)로 교체(jsonld.ts+page.tsx). brand.region 전국구화. llms.txt·layout SEO 문구·render-cards 천안 제거. (천안 유지: brand.company/address 푸터값 + 채용 페이지)
  - ArticleCard·뉴스목록 라이트화(다크 무지개 색맵·골드 알약 배지·slate 제거 → 세리프·골드 헤어라인·언더라인 탭).
- **[Task 2] 딥그린 전면 통일 + 리드 훅**
  - 진단(/diagnosis): 점수 카드(티저+공개)를 딥그린 밴드로, 주CTA 딥그린 채움, 스펙 정리(골드 격하·세리프·radius·tabular-nums). 리드 데이터 흐름은 불변.
  - 뉴스목록: 기사 목록 하단에 **딥그린 리드 훅 밴드**(→ /diagnosis "무료로 내 보험 진단받기") 추가. 유입→리드 다리.
  - 채용(/recruit): 검정 그라디언트 제거→딥그린 CTA 밴드. 히어로 2단(좌 선언 / 우 **딥그린 증거 카드**: {years}년·8년연속·불완전판매 0건·GA명장). CTA는 **카카오톡 채널로 연결**(고객 상담폼과 분리). 천안 유지.
- **히어로 카피 정리**: 뉴스 서브헤드 간결화 / 홈 히어로 **성경 인용문(창세기 45:5) 제거**하고 태그라인("당신의 위기보다 항상 한 발 앞서, 가장 선한 금융의 통로") 유지 / 후크 문장 → **"지금 내고 있는 보험료, 제대로 쓰이고 있을까요?"**
- **[트랙 B 인프라] 도메인·검색등록·측정 — 전부 완료**
  - 도메인 `goodfinance.kr` 연결 ✅ (가비아 A레코드 `@ → 216.198.79.1` → Vercel 도메인 추가(Valid) → env `NEXT_PUBLIC_SITE_URL=https://goodfinance.kr` → 재배포. sitemap이 goodfinance.kr 가리키는 것 검증 완료)
  - 구글 서치콘솔: 인증 토큰 `9t3fLGeJ2V0lK6CX6UOMmnSaDjUNwlbAYyFXbd8Cvbc`(layout.tsx) + sitemap.xml 제출 ✅ (sitemap 상태 "가져올 수 없음"은 제출 직후 정상 — 하루 뒤 재확인)
  - 네이버 서치어드바이저: 인증 `3fb5adb2ed10a1f31427c8760b1ca35f1cc56905`(layout verification.other) + sitemap 제출 ✅
  - GA4: `src/components/Analytics.tsx`(env `NEXT_PUBLIC_GA_ID` 기반, next/script afterInteractive) + layout <body> 삽입. env `NEXT_PUBLIC_GA_ID=G-E1WNEKPYNM` → 재배포 → 실시간 방문 확인 ✅

### 이번 세션 (2026-07-10 이어서) — 콘텐츠 엔진 검증게이트·OSMU 캡션·카드 재디자인
- **[커밋 A] 검증 게이트** — `collect-and-generate`: 배치 내 URL/제목 중복제거(`dedupeBatch`) + 관련성 하한선(`RELEVANCE_MIN=10`, off-topic 감점: 주가·코인·와인·연예 등). 실전 트리거에서 **보험 무관 16건 컷**(중앙일보 워크아웃·이케아·오세훈 등 0점) → "적합 기사 없는 날은 안 나감" 실증. 국고채 이중발행 재발 방지.
- **[커밋 C] 생성 프롬프트 강화** — `remodeling_bridge` 필드 신설(유입→진단 리드 다리; 프롬프트+스키마 required+DB insert). 본진 2,000~3,200자 상향(물타기 금지 원칙). `key_points` 정확히 3개 강제.
- **[커밋 H] 인스타 캡션 5블록** — `instagram_caption` 필드 신설(후크 첫줄·1인칭 공감·✔요약3·CTA질문·해시태그5). 2026 알고리즘(저장·댓글·더보기 유도).
- **[커밋 E] 관리자 페이지** — 인스타 캡션 원클릭 복사 버튼 + 카드 썸네일 그리드(다운로드). `admin/page.tsx` select에 `instagram_caption·carousel_json·key_points` 추가.
- **[커밋 F/J] 카드뉴스 재디자인** — 다크(#050505)+골드범벅 → **크림↔딥그린 리듬**(훅·CTA 딥그린/본문 크림), 골드 헤어라인·형광펜만, "프라임에셋 140본부" 제거→"신순주·GA명장"/"비대면·전국". **장수 내용기반**(5~9, 물타기 금지) + `big_number`(훅 거대숫자) + `highlight`(형광펜). `CAREER_START`는 `getCareer()`와 동기화 주석.
- **폰트 깨짐(한글 tofu □) 수정** — Pretendard static CSS가 `unicode-range` 서브셋이라 `document.fonts.load`에 **실제 렌더 텍스트를 인자로** 넘겨야 한글 서브셋 로드(+`waitUntil:"load"`). `networkidle0`은 CDN 스트리밍에서 60s 타임아웃이라 불가. 추가로 `.hl` CSS 스코프(`h1 .hl`→`.hl`)로 **본문 형광펜**도 표시.
- **key_points 스키마 버그 수정** — Anthropic structured output이 array `minItems≥2` 거부 → 커밋 C가 넣은 `minItems:3/maxItems:3` 제거(개수는 프롬프트·description으로 강제). **라이브 `/api/factory/generate` 400 방지**(아직 게이트 통과 기사 없어 미표면화였음).
- **수입차 기사 carousel 재생성**(일회성 GH Actions, UPDATE만·중복 INSERT 없이) — 새 프롬프트로 10→**7장**(물타기 제거)·`25%` 거대숫자·형광펜 **4/5**·캡션 생성 검증(저장 PNG 실물 확인). 임시 워크플로/스크립트 사용 후 삭제. `ANTHROPIC_API_KEY`는 `.env.local`+GH 시크릿 등록.

### 이번 세션 (2026-07-11~12) — 발행 통제·카드 대수술·소스 대수술·운영 도구화 + ★첫 기사 라이브
- **[커밋 D] 발행 게이트** (`ba156fe`) — 생성=초안(`auto_publish` 기본 false), 발행=/admin 승인 1클릭. 노출 게이트 `is_main_published AND published_at<=now()`를 `articles.ts` 헬퍼 2개에 명시 → **8개 노출지점(목록·상세·메타·정적생성·관련글·홈·sitemap·llms.txt) 전부 수렴**. 예약발행은 `published_at` 미래값으로 크론 없이 동작.
- **[§9] CONTENT-STRATEGY 그라운딩 소스 규칙** (`ebbb46b`) — 공공 1차 소스만 재가공, 비교공시=사실 인용만, 판례·분쟁=사람검수, 민간 취재물(보험신보 등) 재가공 금지.
- **[커밋 카드] 8종 타입 카드뉴스 시스템** (`4940036`) — hook/points/table/steps/stat/callout/quote/cta. 장수는 콘텐츠가 결정(정보 단위 추출→타입 배정, 물타기 금지). 수입차 기사 재생성으로 6장·5타입 혼합 실물 검증.
  - ⚠️ **학습: Anthropic structured output은 optional 필드 총량(~24) 제한**("Schema is too complex" 400 실측) → 타입별 전용 필드 금지, **공용 필드(items/body/extra)에 타입별 의미 매핑**. headline·body는 required로 강제(모델이 caption·correction 누락하는 문제 실측).
- **[커밋 G] 파이프라인 관측성 + 카드 검증 가드** (`5fde847`) — 텔레그램 실측 요약(수집 소스별·게이트 통과/컷·컷 상위 5건 점수·생성 건별 카드 장수, 0건이면 ⚠️로 사유 명시). route.ts 카드 검증 가드(hook 시작/cta 종료/동일 타입 3장 반복/5~9장/필수 필드) — 실패 시 1회 재생성, 최종 실패 시 저장+⚠️플래그.
- **[커밋 B] ★ 콘텐츠 0건 해소 — 소스 대수술** (`e99dad4`) — **금감원 RSS 완전 사망 발견**(HTML 에러페이지 반환 — 4일째 0건의 근본 원인). 게시판 HTML 파싱 전환 + 보험 특화 공공소스 5종(소비자경보·보도자료·꿀팁200선·파인톡톡·민원사례) + 깨진 RSS 방어(에러페이지 감지·엔티티 sanitize) + EUC-KR charset 대응. 실측: **수집 56 → 게이트 통과 5(보험 4건) → 초안 1건 생성**. dedupeBatch 버그 수정(게시판 링크 쿼리=식별자).
  - ⚠️ 제약: 꿀팁200선(PDF 첨부형)·파인 2종(이미지형)은 본문 없음 → 스킵(제목만으론 팩트 없는 원고 = 컴플라이언스 리스크). **금감원 오픈API 키 오면 해결.**
- **[커밋 K] 진단 결과 넛지** (`215268c`) — 점수→패턴(응답 프로필 매핑 테이블, 추정 표현만)→"본인도 모르는 계약이 섞여 있는 경우가 대부분"→전계약 조회 정밀분석→**카톡 CTA**(주 CTA 1개) + 채널추가 약한 연결. **GA4 퍼널**(diagnosis_start/complete/lead_created/kakao_cta_click) + UTM→lead_source 저장.
- **[핫픽스] SSG 404** (`34f31b2`) — 빌드 시점 초안이던 글은 발행해도 상세 404. `/news/[slug]` dynamicParams+revalidate 60 + admin 발행/회수/삭제 시 revalidatePath → **발행 즉시 반영, 회수 즉시 숨김**. 더미 초안으로 404→발행 200→회수 404 실측.
- **[프롬프트] "신순주 지사장의 한 줄 조언"** (`65e8789`) — brand.ts 싱글소스(`${personName} ${title}`). ⚠️ 브랜드명 "신순주의 선한 금융"은 치환 금지.
- **[커밋 I] /admin 운영 콘솔** (`13ab1e9`) — 매일 5분 운영 도구화: 본진 원고 미리보기 모달(마크다운 렌더+summary·핵심3줄·리모델링다리), 리드 삭제·메모·상태 5종·연락처 복사, 카드 라이트박스(←→/ESC)+ZIP 일괄 다운로드(jszip), 배포 체크리스트(네이버/블로그스팟/인스타 토글+미배포 필터), "오늘 할 일" 요약 바(미검수 초안/미배포 발행글/새 리드), 기사 삭제 시 Storage 카드도 정리(/api/admin/delete).
- **[커밋 L] OSMU 원고 포맷 자동화** (`5c02af4`) — 복사 시점 변환(DB 원본 불변): 블로그스팟=마크다운→HTML(h2·ul·ol·table)+이미지 마커 제거+본진 언급을 기사 앵커로 / 네이버=마크다운 기호 제거(이미지 마커 유지)+말미 진단 URL 삽입. 부당승환 실데이터로 검증.

### 채널 (2026-07-12 신규 개설·정비)
- 인스타 `@goodfinance_sj` — 프로필 완성. 이름필드 "신순주 | 보험 리모델링 · 보장분석"(검색 최적화)
- 네이버 블로그(insightlab-daily) — 블로그명 "신순주의 선한 금융 | 보험 리모델링", 별명 "신순주 지사장", 카테고리 6개(리모델링 1번), 상단메뉴 4개, 딥그린 타이틀 배너, 프로필 사진
- 블로그스팟 `goodfinance-sj.blogspot.com` — 개설, 검색노출 ON, 첫 글 게시
- ★ **첫 기사 라이브 발행: "보험 갈아타기 권유, 부당승환 소비자경보 발령"** (카테고리: 보험 리모델링 — 승환 경고라 브랜드 서사와 정확히 일치)

### 이번 세션 (2026-07-12) — ★[A-2] 상록수 파이프라인 완성 + 허브글 4채널 발행
- **커밋 M0** 뉴스 본문 길이 가드 — 글자수 지시는 안 먹힘(1,749자 실측). 구조로 강제(H2·표·FAQ 수) + 1회 재생성
- **커밋 M1** 스키마 — content_type / seed_key / verify_claims / needs_human_review (+ naver_title / blogspot_title)
- **커밋 M2** evergreenSystemPrompt + generate mode 파라미터 + 사실검증 게이트(자가 플래그 + 정규식 하드룰)
- **커밋 M3** 시드뱅크 22개 + generate-evergreen.mjs + 화·금 워크플로(evergreen-content.yml)
- **커밋 M3-1** 경어체 통일 + 그라운딩 오염 차단(불용토큰·폴백 제거)
- **커밋 M3-2** 리모델링 브랜드 정의 하드코딩 + 유통채널 내부사정 금지
- **커밋 M3-3** 전문가 판단 프레이밍 — 셀프체크 인상 제거, '대부분 유지' 편향 제거
- **커밋 M3-4** 용어사전·도입훅·담보합산표 의무화 (수동 교정분 프롬프트 환원)
- **커밋 M3-5** 채널별 제목 3종 분리(자기잠식 방지) + 블로그스팟 H2 소제목
- **커밋 M3-6** 태그 자동생성 + 채널별 CTA 분리 + 본진 링크 + 제목 길이 + 일화 날조 금지 + 카드 렌더러 2건
- ★ **허브글 「보험 리모델링 완벽 가이드 — 새는 보험, 담보 단위로 잡는 법」 4채널 발행**
  (본진 4,242자 / 네이버 / 블로그스팟 / 인스타 카드 9장)
- ⚠️ **부당승환 뉴스 기사 회수** — GA를 가해자로 지목(신순주 본인이 GA 소속). 블로그스팟에서도 삭제.

### 이번 세션 (2026-07-13) — 리드 퍼널 완성 + 콘텐츠 엔진 컴플라이언스

**[진단 페이지 전면 재설계 — 웹의 유일한 전환 장치]**
- N1 담보 합산 목업 + 잔여 공백 경고
- N2 ★리드 게이트 제거 — 점수를 인질로 연락처를 받던 구조 폐기.
  "웹의 KPI는 리드 수집이 아니라 상담 성사"(CLAUDE.md) 위반이었음
- N3 CTA 카톡 1개로 단일화 — "부담스러우시면" 류 이탈 구멍 4개 제거.
  리드 폼 삭제, 익명 진단 로그로 대체(PIPA 안전)
- N4 목업을 실제 분석 2건(익명)으로 교체 — ★보험료 감소 사례 + 증가 사례 병렬
- N5 기사 CTA 두 갈래 — 글 중간(각성 정점) + 글 끝(딥그린 밴드)
- N6 상록수 프롬프트에 인라인 CTA 마커 자동 삽입
- N7 진단 문항 4개 → 7개 재설계 + 소득 대비 비율 판정 + 점수 근거 공개
- N8 응답별 목업 도입 문장 (계약 개수에 따라 논리 연결)
- N9/N9-1 대비 밴드·정렬 위계·선택지 질감 / 인증 밴드를 질문 위로
- O4 ★목업을 '채워준 결과' 중심으로 전면 교체 ("없음 → 1억원")
- O5 딥그린 결론값 · 세리프 숫자 · 점수 카드 severity 위계
- O6 결론 블록 3단 위계 + 카드 행 단위 높이 정렬 (실측 Δ=0px)
- O7 퀴즈 연타 스톨 버그 수정 (60ms 연타 5회 완주 검증)

**[콘텐츠 엔진 — 컴플라이언스 사고 수습]**
- O1 ★금지소재 3겹 방어 — 어제 회수한 부당승환 기사가 같은 소스로 재생성됨.
  본문에 1,200%룰·정착지원금·판매수수료가 그대로 들어감(GA 소속인데 GA 폭로).
  → 공통 프롬프트(생성 전) + 하드 게이트(저장 전) + blocked_sources(수집 전)
- O2 img alt 수확 — 본문없음 25 → 13 (민원사례 8→0 전량 회수)
- O3 보도자료 PDF 3차 폴백 — 13 → 12 (boost 4 최상위 소스 회수)
- O8 ★M3 규칙 전수 공통화 — 상록수에만 있던 규칙이 뉴스에 안 내려가 있었음.
  경어체·리모델링 정의·용어사전·전문가 프레이밍·컴플라이언스를 공통 베이스로.
  [상록수 전용] 판정: 담보합산표 의무화·도입훅·CTA 마커·verify_claims

### 이번 세션 (2026-07-13 오후) — 마케팅 퍼널 설계
- **P0** marketingskills 3종 설치(ai-seo / marketing-loops / lead-magnets) + CLAUDE.md §9.
  competitors(비교·순위 금지)·popups(에디토리얼 충돌)·ab-testing(트래픽 0) 의도적 미설치.
  스킬 제안이 헌법·컴플라이언스와 충돌하면 헌법이 이긴다.
- **P1** 벤치마킹 환원 — 생애 이벤트 시드 4종(22→26: 신혼·출산·퇴직 전 5년·이직 소득변화)
  + 공통 프롬프트 제목 훅 패턴 3종(통념 반전·질문·숫자 구체, 낚시 금지).
  실측: "보험 리모델링" 검색 상위는 전부 기업(시그널플래너·KB Think·언론),
  실명 개인 전문가 부재 + 담보 '합산 금액'을 보여주는 곳 없음 = 우리 빈틈 확인.
  신규 시드 로컬 생성 테스트: 훅·경어체·담보합산표 전 항목 통과.
- **P2** /admin 지식iN 답변 어시스트 — kin_answers 테이블·초안 생성 API(공통 베이스 상속
  프롬프트)·이력 UI(상태 4종·원클릭 복사·posted_at 기록). **자동게시 경로 자체가 없음**(어뷰징 방지).
  가상 질문 검증: 475자·경어체·상담 권유 0건·무조건 해지 부정·승환 리스크 3종 언급.
- **P3** 채널별 퍼널 맵 §10 + 관계 장치 카톡 단일화 —
  본진 글끝 카톡 채널 보조 링크(GA4 kakao_channel_add_click) ·
  네이버 CTA 배너를 채널 추가 카피로 교체 · 변환기 말미 카톡 URL 1줄 ·
  뉴스레터 후순위 강등 · 셀프체크 PDF 폐기(1호=실손 청구 서류 체크리스트로 대체).
  + render-cards 실측 버그 수정(인스타 0건 시 조기 return이 네이버 전용 글을 영영 스킵).
- **P4-1** 리드마그넷 전면 폐기 — 퍼널의 미끼는 자료가 아니라 진단(각성).
  정보 수요(청구 서류 등)는 상록수 시드가 커버.
- **P5** 종목별 가이드 시드 5종(26→31) — 운전자·치아·종신·일상배상·단체.
  검증 시드를 일부러 과거 오염 사고 주제(일상배상)로 지정 → 정주제 그라운딩 청정 확인.
- **O9** 뉴스 관련성 게이트 3축 재구성 — 불용 토큰('보험' 만능 매치 차단) +
  소비자 접점 필수(6개 카테고리 전부 커버, 좁힘 없음) + 지역 고정·공급자
  내부 관점 감점. 회귀 41건: 사고 유형 컷(-34)·소비자경보류 컷→통과 6건·
  과잉 컷 0. --score-only 드라이런 플래그 상설화.
- **O10-A** 소스 정찰(probe-sources.mjs 상설화) — 6곳 라이브 실측:
  금융위·복지부 붙인다 / 손보협회·보험연구원 보류 / 생보협회(JS POST)·
  건보공단(robots Disallow) 탈락. 경제지 RSS는 §9 민간 취재물 원칙으로 제외.
- **O10-B** 복지부 RSS + 금융위 보도자료 배선 — 드라이런: 각 8건 수집·
  2건 통과, 분포 상식적(지역금융 간담회 -10 강컷), 기존 소스 회귀 0.
  내일 크론부터 소스 7종 체제.

### 이번 세션 (2026-07-13 저녁) — /admin 상록수 운영 도구 + 일화뱅크 + 버그픽스
- **M4-1** /admin 상록수 서브탭(뉴스/상록수 분리) + seed_key 칩 + 🔴검증 필요 배지
  + 모달 사실검증 체크리스트(verify_claims 7건 렌더) + 발행 confirm 게이트
  (발행 시 needs_human_review 해제 — 예약발행에도 동일 게이트, 우회 방지). (9605d68)
- **M4-2** 상록수 시드 → 허브글 내부링크 결정적 삽입 — 첫 H2 직전 인용 블록,
  멱등, 채널 원고 미삽입(네이버 스팸 판정 회피), 허브 미발행 시 생략+⚠️.
  구조 확인: insert는 /api/factory/generate가 하므로 생성 직후 service-role
  UPDATE 후처리 방식. 텔레그램 🔗 라인 = 삽입 관측 장치. (5bb5773)
- **일화뱅크** scripts/anecdote-bank.mjs — 진단 페이지 실사례 2건
  (mockup-analysis.ts 수치 그대로: 감소 505,921→473,983 / 증가 405,026→465,442)
  구조화 + 시드별 매칭 주입. 매칭 0건이면 "일화 사용 금지" 지시(폴백 금지 동일 원칙).
  + 날조 감지 하드 게이트(특정 시점+고객 결합 정규식 → needs_human_review 플래그,
  뉴스·상록수 공통). 텔레그램 🧾 라인. (08c57b9)
- **버그 1** /admin [초안으로] — 재현 결과 API·revalidate 정상, 실체는 updateRow
  실패(401 등) 시 낙관적 UI가 실패를 가리는 무음 실패. 발행 컨트롤 3종에
  결과 검사 + 롤백 + 토스트. (3f39ddd)
- **버그 2** 텔레그램 리드 알림 ���� — 발신 코드 정상, 원인은 비UTF-8(CP949)
  유입을 request.json()이 U+FFFD로 디코딩해 DB·알림까지 전파.
  유입 시점 U+FFFD 검사 400 차단 + 전송 헤더 크론과 통일. (d090ea0)
- **상담관리** /admin 상담 신청 관리 UI(ConsultCard) — 상태 5종·메모·삭제
  (개인정보 파기 수단, §6)·연락처 복사. 리드(커밋 I) 패턴 재사용,
  무음 실패 3종 방어. memo 컬럼은 MCP 마이그레이션. (27fab56)
- **운영(코드 아님)**: 허브글 검증 7건 실검수 → 🔴 해제(M4-1 게이트 첫 실사용 완료)
  + 배포 체크 3채널 / 테스트 상담 행 삭제(MCP) / 인스타 프로필 링크 → /diagnosis
  / 빙·다음 검색등록 제출(검색등록 4종 체제) / 카톡 비즈니스 심사는 사업자 등록 후 대기

### 이번 세션 (2026-07-14) — 첫 실전 가동일, 조용히 망가져 있던 것 3개 발견

**[콘텐츠 엔진 — 구조적 결함 3건]**
- **수집 캡 4,000자 → 12,000자** (e69ea73) — ★원문의 62%가 잘린 채 매일 생성되고 있었음.
  파인 민원사례는 사례를 이어붙이고 판단결과를 뒤에 두는 구조라, 캡이 '결론'만 골라서 잘랐음.
  실측: 원문 5,441자 중 모델이 본 것 4,000자, 검수자가 본 것 2,000자(37%).
  후보군에 10,826자 원문도 있었음(62% 손실). 스코어링 입력은 4,000자 고정(O9 회귀 0 — 채점표 diff 0줄).
- **상록수 thinking 폭주** (f621328) — ★상록수는 한 번도 성공한 적이 없었음.
  Sonnet 5는 thinking 미지정 시 adaptive thinking 기본 ON → 출력 16k 중 14.5k가 추론,
  본문 1,473자에서 잘림(311초). `thinking:{type:"disabled"}` 명시 + 스키마 2단계 분리
  (1차 본진 / 2차 채널·카드). 결과: 120초 완주(1차 59초 · 2차 61초 · 재생성 0).
  지식iN 라우트(max_tokens 1,500)도 같은 이유로 답변이 통째로 잘리고 있었음 — 함께 수정.
  ⚠️ Vercel Hobby maxDuration 300초는 상한(800 배포 실패 실측) — 올리려 하지 말 것.
- **뉴스에 사람검수 게이트 자체가 부재** (e63b550) — 상록수에만 있고 뉴스엔 일화 날조 외
  아무 사실검증 플래그가 없었음. 분쟁사례 기사가 관련도 116점 최우선 선정 → 무플래그 자동 생성.
  scanHighRiskTopics 추가(분쟁·판례·세법·의료 토큰 → needs_human_review 강제 + verify_claims 자동 주입,
  텔레그램 ⚖️ 라인). 뉴스·상록수 공통 지점.

**[검수 인프라]**
- **검수용 원문 전문 보존** (0f0b398) — `raw_source_fulltext` 컬럼 + /admin [원문 보기]
  좌우 대조 뷰. 기존엔 원문 URL 접근 불가 + excerpt 2,000자 절단이라
  🔴 체크리스트가 대조 대상 없이 형식만 남아 있었음(= 검수 연극).
  원문 미보존 기사는 ⚠️ 경고 + 버튼 비활성화.

**[비용]**
- **프롬프트 캐싱** (2796b55) — system 블록 배열 + `cache_control`(ephemeral).
  실측: 1회차 쓰기 14,491토큰 / 2회차 읽기 14,491토큰, 입력의 97.8%가 캐시 대상.
  텔레그램 💾 관측 라인(0/0이면 ⚠️ 노출).
  ⚠️ 상록수 2차 호출은 스키마가 달라 캐시 미공유(읽기 0) — 관측 장치가 스스로 잡아냄. 대기 항목.
- **청구 구조 정리** — Claude Code는 Max 구독(Login method: Claude Max account 확인),
  API 크레딧은 크론 전용. 5일간 총 $5.21 + 오늘 폭주 $1.79 = $7.00. 하루 실비 $1 수준.
  자동 리로드 OFF 상태 — 잔고 0이면 조용히 죽으므로 주기 확인 필요.

**[브랜드·카피]**
- **카톡 '새 글 알림' CTA 전면 제거** (db32a19) — 카톡 채널은 아직 아무 발송도 안 하는데
  "새 글 알림 받기"를 약속하고 있었음(P3에서 진단 배너를 카톡 배너로 교체한 것이 원인).
  네이버 변환기·카드 배너·본진 글끝 3곳 제거 → 진단 배너로 원복.
  상담용 카톡(푸터·채용·진단 결과)은 유지(실제로 사람이 응답하므로).
  + 폰트 서브셋에 라틴 글리프 추가(배너 문구 변경 시 글리프 누락 → 폴백 렌더 사고 방지)
- **제목 생성 규칙 명문화** (db32a19) — 오늘 3채널 제목 6개를 전부 사람이 갈아엎음.
  【필수】검색어 포함 · 소비자 실익/통념 반전 · 본문과 일치 /
  【금지】관공서 요약체("~에 대한 확인사항") · 시비조("안 알려줬다고요?") · 낚시.
  ★기존 훅 유형 ①이 "◯◯라고요? ◯◯부터입니다"를 명시적으로 지시하고 있었음 —
  "써라"와 "쓰지 마라"가 공존하던 충돌 제거.
  + "관공서 원문 제목투를 답습하지 말고 소비자 언어로 번역한다"
- **한 줄 조언 인용 블록 렌더 버그** (009ed2b) — `> 제목\n> 본문`이 마크다운에서
  한 문단으로 합쳐짐 → `> **제목**\n>\n> 본문`으로 형식 강제(공통 베이스).
- **사이트 URL fallback 고정** (3c7bd78) — brand.ts fallback이 soonjoo.vercel.app이라
  env 누락 시 유령 도메인 링크가 외부 채널로 나갈 뻔함 → goodfinance.kr로 고정.
- **sitemap 색인 파이프 진단** — 정상 확인(XML·헤더·robots·도메인 전부 이상 없음).
  PROGRESS의 "sitemap `<script/>` 3줄 제거" 대기 항목 폐기.

**[발행 — 첫 3채널 배포]**
- 「연금, 60세보다 일찍 받을 수 있습니다 — 단 조건 2개」(뉴스) 발행 + 네이버·블로그스팟
- 「소액사고인데 보험료가 올랐다면 — 자동차보험 할증 오해 3가지」(상록수) 발행 + 네이버·블로그스팟
- 인스타는 스킵(주 3~4회 리듬 / 카드가 옛 제목 기준)
- 현재 발행글 3건

---

## ⏭ 다음 우선순위 (2026-07-14 마감 기준 — 첫 실전 가동 결과 반영)

1. ★ **시드 의도 준수 강제** — 'car-insurance-savings'(절약) 시드인데 본문 70%가 할증.
   그라운딩 소스에 할인 정보가 없어 있는 소스(할증)로 글이 끌려감.
   프롬프트에 "시드의 검색 의도를 본문 중심축으로" 명시 + 소스 부족 시 실패 처리(폴백 금지 연장).
   ⚠️ 유입 자석(절약·꿀팁)이 실제로 안 만들어지고 있다 = 검색 유입의 근본 문제.
2. **일화뱅크 확충** — 지사장님 질문지 발송 완료, 답변 대기.
   7번(보장이 작동한 사례 + 왜 그 담보를 넣었는가) / 8번(못 넣어드려 아쉬웠던 것)이 핵심.
3. **한 기사 = 한 주제** — 원문에 사례 여럿이면 분리(연금+배상책임이 한 글에 섞임)
4. **네이버 인용 블록 첫 줄 ✓ 누락** — 변환기 버그 의심(재발 시 확정)
5. **생성 한도 2 → 상향** — 원료는 게이트 통과 21건인데 2건만 씀. 캐싱 깔았으니 지금이 타이밍.
   병목은 검수 캐파 → 하루 검수 가능 편수 확정 필요.
6. **O10 신규 소스 2종 수집 실패** — 복지부·금융위. 로컬(한국 IP) 성공 / GH Actions(해외 IP) 실패.
   fetch failed의 err.cause를 러너에서 직접 확인해야 함. ※ 원료는 남아돌아 급하지 않음.
7. **💾 상록수 2차 호출 캐시 미적중** — 스키마가 프리픽스에 포함돼 단계 간 공유 불가.
8. 정리: diagnosis-patterns.ts 미사용 파일 삭제

> ✅ **검수 배율(07-10 결정, 커밋 D가 실행 장치):** 절약·청구·실손·연금·리모델링 = 검수 후 발행 / 판례·세법·의료 = 사람검수 필수. 본문에 구체 숫자(세율·법령·의료수치) 감지 시 ⚠️검증필요 플래그(추후).

## ⏭ 그 이후 (순서)
- **상록수 생성 가속(주 4~5편)** — 화 첫 자동 실행 검증 통과 후.
  생성은 수동 트리거로 몰아서, 발행은 예약발행으로 간격. 병목은 검수 캐파.
- **premium_articles에 네이버 게시 URL 컬럼** — 지식iN 답변 링크 드롭다운(현재 수동 입력)·배포 체크리스트 연동
- **카톡 채널 주 1회 발송 시작** — 수동 운영(코드 아님, §10). 발송 요일 확정 대기.
  ※ 발송이 실제로 시작되면 그날 '새 글 알림' CTA 3곳(네이버 변환기·카드 배너·본진 글끝)을 되살린다(07-14 제거분).
- **인스타 발행 큐** — 카드는 매일 생성되나 인스타는 주 3~4회만 발행 → /admin에 적합도 표시(정보 밀도 높은 table/steps/points/stat 위주) + 미발행 큐 필터. 카드가 쌓인 뒤 착수.
- **DB 전반 토픽 유사도 중복제거** (현재는 배치 내·URL만 — 같은 주제 이중 발행 방지)
- **보험 특화 소스 추가 확장** (생·손보협회·보험연구원 등 — §9 규칙 준수)
- **채널 배포 자동화 마감**(OSMU 텔레그램 최적화 등).

## 🔧 자잘한 대기 (급하지 않음)
- CHANNEL-PLAYBOOK.md 현행화 또는 폐기 — 내용이 낡아 다른 문서와 충돌(도메인 vercel.app,
  카테고리 5개, 인스타 매일 발행, 네이버에 카드뉴스 첨부 등). 채널 전략은 CONTENT-STRATEGY(발행 리듬·
  인스타 유형 4종)와 CLAUDE.md §4(채널별 이미지 설계)가 흡수함. 프로젝트 지식에서는 제외.
- 빙·다음 검색등록(선택).
- 상담폼: 카톡 채널 버튼 추가. (제출 버튼 색 문제는 N3에서 진단 리드 폼 자체를 제거하며 해소)
- daily-content.yml 말미의 일반 "✅ 완료" 텔레그램 스텝 제거(커밋 G 실측 요약과 중복).
- 카드 생성 변동성 모니터링 — 검증 가드(커밋 G)가 잡지만, ⚠️ 플래그가 잦으면 프롬프트 보강.
- 상록수 도입훅 하드 가드(정규식) — P5 검증에서 용어 정의 시작 1건 관찰, 재발 시 승격.

## 📣 별도 세션 예정 — 광고·마케팅 유입
- 유료광고(네이버 파워링크·메타·카카오) + 신뢰기반 훅. 보험광고 컴플라이언스 + 리드DB 퍼널 + PIPA.

---

## 🧭 핵심 결정 메모
- 6 카테고리 확정(위). 판례·분쟁은 자동 제외(반자동·사람검수, 나중).
- 이미지: 인물 스톡·질감 레이어 안 씀. 웹 기사엔 이미지 없음. 카드뉴스는 인스타 전용.
- 상담 동선 카톡 우선(전화 041 보조). 채용 상담은 카톡 채널로(고객 상담폼과 분리). 고객 실데이터 웹 노출 금지(진단 시스템 로컬 전용, 사이트는 목업만).
- 신뢰 카피는 "확인하세요"(고객 숙제) 금지 → "공개합니다/바로잡습니다"(당당). 특정사 비방 금지.
- 커밋은 목적별 분리. 경력 숫자는 무조건 getCareer().
- Vercel: 프로젝트 `soonjoo`(org: SPA LUX's projects). 도메인 goodfinance.kr(주) + soonjoo.vercel.app(유지). env: `NEXT_PUBLIC_SITE_URL`, `NEXT_PUBLIC_GA_ID`, `FACTORY_SECRET` 등.
- Supabase: shin-good-finance (`lgbbflolunlseutvqaso`) — claude.ai 커넥터로 직접 조회 가능.
- 스케줄러: GitHub Actions `daily-content.yml`(매일 06시 KST) — 뉴스 수집→생성(초안)→카드뉴스→텔레그램.
- **데일리 운영 플로우(07-12 확정):** 새벽 크론 → 초안 적재 + 텔레그램 실측 요약(0건이면 사유 표시) → `/admin` "오늘 할 일" → [본문 보기] 검수 → [발행](즉시 반영) → 카드 ZIP + 채널별 변환 복사(블로그스팟 HTML·네이버 텍스트) → 배포 체크박스. 잘못 나간 글은 [초안으로] 즉시 회수.
- 소스 원칙(§9): 공공 1차만 재가공. 본문 없는 소스(PDF/이미지형)는 스킵 — 제목만으로 생성 금지(팩트 없는 원고).
- **길이는 구조로 강제한다.** 프롬프트의 글자수 지시는 모델이 무시함(실측).
- **그라운딩 폴백 금지.** 관련 게시글 0건이면 소스 실패 처리. 폴백이 오염의 근원(펫보험 침투 사고).
- **불용 토큰 주의.** 리모델링 시드는 키워드가 죄다 '…보험'이라 '보험' 토큰이 만능 매치가 됨 → 특이 토큰 필수.
- **본진·블로그스팟 제목이 같으면 구글 자기잠식.** 채널별 제목 3종 분리.
- **수동 교정은 자동화가 아니다.** 글을 고쳤으면 반드시 프롬프트로 환원한다.
- **웹은 증명하고, 상담이 판단한다.** 진단 결과에 연락처 게이트를 두면 가장 뜨거운 순간에 손님을 식힌다. 증명(목업)을 다 보여주고 상담으로 보낸다.
- **목업은 '진단'이 아니라 '처방'을 보여준다.** "비어 있음"에서 멈추면 제안을 수용한 고객이 봤을 때 무능한 설계로 읽힌다. 화살표 오른쪽(채워준 금액)이 본체다.
- **리모델링 결과는 보험료가 줄 수도, 늘 수도 있다.** 한 사례만 보여주면 "리모델링=절감"으로 오해 → 과장광고 + 가격 경쟁 프레임(브랜드 훼손). 감소/증가 두 사례를 나란히 놓아야 "결론이 미리 정해져 있지 않다"가 증명된다.
- **상록수에서 고친 규칙은 반드시 뉴스에도 내린다.** 프롬프트가 분리돼 있으면 같은 사고가 반복된다(문체·금지소재 2회 실측). 공통 베이스에 둔다.
- **기사를 삭제하면 소스가 부활한다.** URL 중복제거가 DB 대조 방식이라 회수할수록 되살아난다 → blocked_sources 테이블로 영구 차단.
- **오픈API가 있다고 본문이 있는 건 아니다.** FSS 오픈API 3종 전부 contentsKor 0자(PDF 첨부형). 붙이기 전에 라이브 호출로 확인한다.
- **밋밋함의 원인은 폰트가 아니라 위계 부재다.** 배경 이미지·그라디언트로 풀지 않는다. 색·굵기·세리프·여백으로 결론을 떠오르게 한다.
- **관계 장치는 카톡 채널 하나로 단일화한다.** 빌린 땅(네이버·인스타)의 팔로워보다 소유 자산의 친구 수가 지표다.
  ⚠️ 단 **발송을 시작하기 전까지 '새 글 알림' CTA는 붙이지 않는다**(07-14 전면 제거). 채널 추가 유도는 주 1회 발송이 실제로 돌기 시작한 날 되살린다.
- **글끝은 진단으로, 상담은 진단 결과가 맡는다.** 단계마다 다음 행동은 하나.
- **리드마그넷은 만들지 않는다.** 우리 퍼널의 미끼는 자료가 아니라 진단(각성)이다. '고객이 준비할 것 없음'이 사업의 차별점 — 웹이 고객에게 자료·숙제를 건네는 장치는 브랜드와 역행. 자료를 인질로 연락처를 받는 구조는 리드 게이트(N2 폐기)의 재탕. 정보성 내용(청구 서류 등)은 상록수 글로 전부 나간다.
- **게이트는 소스보다 먼저 조인다.** 소스 확장은 게이트가 걸러줄 것을 믿고 넓히는 것 — 순서가 반대면 검수 부담으로 돌아온다(O9→O10 순서 실증).
- **robots가 막은 공공 게시판은 크롤링하지 않는다(건보공단).** 대체 소스를 찾는다(복지부 RSS).
- **무음 실패 금지.** 낙관적 UI는 반드시 실패 롤백+토스트와 쌍으로. "눌러도 안 됨" 신고의 절반은 가려진 실패다.
- **깨진 인코딩은 경계에서 차단한다.** 비UTF-8 유입(U+FFFD)은 400 — DB에 들어온 오염은 알림까지 전파된다.
- **텔레그램 요약의 🔗(허브링크)·🧾(일화) 라인은 관측 장치다.** 상록수 요약에 이 라인이 없으면 해당 처리 누락 신호. (07-14 추가: ⏱️ 소요시간 · 💾 캐시 · ⚖️ 고위험 주제)
- **모델의 thinking이 max_tokens를 본문과 나눠 쓴다.** Sonnet 5는 기본 ON.
  구조화 출력 작업에서는 명시적으로 끈다. "출력이 잘린다"의 첫 용의자.
- **수집 캡은 '결론'을 자른다.** 공공 게시물은 사례→판단결과 순서라 뒤를 자르면
  결론만 사라진다. 본문 길이 확보 ≠ 필요한 문장 확보.
- **대조할 원문이 없으면 검수는 연극이다.** 체크리스트보다 원문 보존이 먼저다.
- **없는 서비스를 약속하지 않는다.** 카톡 발송을 안 하면서 "새 글 알림"을 붙이면
  첫 약속부터 어기는 것. 신뢰는 증명하는 것이지 요청하는 것이 아니다.
- **프롬프트 안에서 "써라"와 "쓰지 마라"가 충돌하면 모델은 아무거나 고른다.**
  금지 규칙을 추가할 때 기존 지시에 같은 패턴을 시키는 예시가 없는지 반드시 확인한다.
- **제목은 본문의 중심축과 일치해야 한다.** 시드가 '절약'인데 본문 70%가 '할증'이면
  제목을 바꿀 게 아니라 본문이 시드 의도를 벗어난 것이다(→ 시드 의도 준수 규칙 필요).

## 작업 방식 (워크플로우)
- 사용자는 Claude 웹(상의·감수)과 클로드코드(로컬 D:\dev\SHIN 구현)를 함께 쓴다.
- 흐름: Claude 웹과 상의 → **patch 또는 복붙 명령**(사용자가 patch 다운로드 귀찮아하면 복붙 명령 선호) → 클로드코드 실행·push → Vercel 자동배포 → 스크린샷 공유 → 확인 → 다음.
- Claude 웹은 카피·디자인·컴플라이언스를 감수. 문서(헌법/전략/PROGRESS) 변경 시 프로젝트 지식+레포 양쪽 동기화.
- Claude 웹은 프로젝트 지식만, 클로드코드는 레포만 본다 → 기준 문서는 양쪽에 둔다.
