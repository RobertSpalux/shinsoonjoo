# WORKFLOW-EVERGREEN-B — 건강+보험 상록수 반자동 제작 절차

> 건강+보험 롱테일 상록수를 **Claude 웹 채팅에서 반자동(B)**으로 만드는 절차.
> 주제는 TOPIC-BANK.md, 브랜드·컴플라이언스는 CLAUDE.md, 콘텐츠 전략은 CONTENT-STRATEGY.md를 따른다.
> 최종 갱신: 2026-07-20 (광고심의 10단계 편입 · 심의 캡처 3종 도구 반영 · STEP 재번호 1~8)

---

## 왜 B(반자동)인가

- 건강+보험 글은 사실 하나 틀리면 컴플라이언스로 신순주가 다친다 → **어차피 사람 검수 필수.**
- 상록수는 하루 1편 수준 → 완전 자동(크론+API 웹검색)의 이득이 작다.
- B는 **Claude 웹 검색(구독제)** 을 쓰므로 **API 검색 비용 0**, 코드 작업 없이 즉시 가동.
- Claude가 로버트와 대화하며 쓰므로 품질이 더 높다(근거 확인 → 초안 → 즉시 교정).
- ⚠️ 완전 자동(A: 크론이 API 웹검색으로 혼자 생성)은 **검수 캐파가 안정되고 발행량이 늘어난 뒤** 별도로 검토. 지금은 B.

## 발행 리듬 상한 (⚠️ 심의 한도가 병목 — 트랙 B 속도를 결정한다)

- 동시 심사중 **2건 한도.** 2건이 걸려 있으면 추가 신청이 불가하다.
- 심의 소요 영업일 1~2일.
- ⚠️ **[미확정]** 동일 원고를 네이버·블로그스팟·웹사이트 3곳에 올릴 때 1건인지 3건인지 미확인.
  **보수적으로 3건 가정** → 1편 = 3건이면 동시 2건 한도에 1편도 못 들어간다.
- 따라서 **첫 심의 판정이 나오기 전까지 발행량 계획을 세우지 않는다.**
  판정이 3건으로 나오면 채널 축소가 필요하다(블로그스팟이 1순위 후보 — SEO 보조 역할).
- 유효기간은 심의일 + 1년. 연장 기한은 바이럴 기준 **만료 + 90일.**
  `ad_reviews_expiring` 뷰(만료 60일 이내)를 주 1회 확인한다.

## 구독제 웹검색 vs API 웹검색 (혼동 주의)

- **구독제 웹검색** = 이 채팅에서 로버트가 Claude에게 말 걸 때 Claude가 검색. 사람이 있어야 작동. 비용 0. → **B가 이걸 쓴다.**
- **API 웹검색** = 크론이 새벽에 혼자 검색. 사람 없이 작동. 비용 있음. → A 전용, 지금 안 씀.
- 둘은 별개. 구독제로 크론을 돌릴 수는 없다.

---

## 제작 절차 (1편 기준)

**STEP 1 — 주제 선택**
- TOPIC-BANK.md에서 ⬜ 미작성 주제 1개 선택. 시즌 주제(S)는 해당 계절 우선.

**STEP 2 — 근거 확보 (Claude가 웹 검색 → 증빙 PDF 확보)**

⚠️ 심의 체제(§6.10): 근거는 "읽기용"이 아니라 **심의에 제출할 수 있는 형태**여야 한다.
교차확인만으로는 부족하다 — 원문 PDF를 제출 못 하면 그 문장이 반려 사유가 된다.

- 증빙은 **원출처 기관이 발행한 자료 원문**이어야 한다.
  (복지부·금융위·금감원 보도자료, 통계자료, 표준약관, 법령 원문)
- ❌ 증빙 불가: 기사 본문, 네이버 지식백과, 위키, 블로그, **보험사 뉴스룸.**
  기사는 **대제목(헤드라인)만 인용 가능**하다. 대제목에 통계치가 있으면 원출처 자료를 제출한다.
- 원문 PDF를 반드시 내려받아 보관한다. 파일명 규칙(**4요소, 1개라도 누락 시 반려**):
  `작성기관명, 자료명, 기준년도, 발표연도.pdf`
  예) `금융감독원, 2023년 실손의료보험 사업실적, 2024.5.9.pdf`
- **원문 PDF를 확보할 수 없는 수치는 본문에서 뺀다.** (교차확인만으로는 부족)
- ⭐ STEP 2의 산출물은 "근거 메모"가 아니라
  **증빙 PDF 파일 묶음 + 각 수치가 어느 파일 몇 쪽인지의 대응표**다.
- 최신성: 기본 2년 내 자료. 없으면 출처 하단에 대체 사유를 기재한다.

**STEP 3 — 초안 작성 (Claude)**
- 기존 상록수 규칙 그대로 적용:
  - 경어체, 담담한 톤, 23년 현장 전문가 목소리
  - 리모델링 정의 준수(갈아타기·승환과 등치 금지)
  - 담보 합산은 금액+합계로(해당 시)
  - **AEO 구조**: 질문형 제목 + FAQ 섹션(구글·네이버 AI 인용 대응)
  - 글 끝 진단 CTA + (리모델링 관련 시) 허브 내부링크
    · 이 CTA는 **본진(웹사이트) 한정**이다. 외부 채널 원고에는 URL을 넣지 않는다
      (osmu-format `includeDiagnosisCta` 기본 off).
    · ⚠️ 본진 CTA는 '빼는 요소'가 아니라 **심의 캡처에 포함된 채로 통과해야 하는 유인 요소**다.
      웹사이트는 광고형태 "홈페이지"로 심의받는 광고물이고, CTA는 그 광고물의 일부다.
    · 따라서 CTA 문구도 §6.10 금지 단어 검사 대상이다. CTA는 **"보험 리모델링 진단"으로 통일**한다
      ('무료' 제거 완료 — 생성 프롬프트·osmu-format·렌더러·웹 전면 sweep).
  - 셀프체크 인상 금지 — 전문가가 왜 필요한지 남긴다
- 컴플라이언스 (§6.10 — 위반 시 반려):
  - **금지 단어**: 무료 / 끝판왕 / 최고 / 최대 / 무려 / 단점 / 함정 / 허점 / 치명 /
    보험료 폭탄 / 무조건 / 다이렉트 / 증빙 없는 백분율
  - **제목에 금액 노출 금지.** 본문에서는 가능.
    (예: "내 암보험금, 진짜 5,000만원 맞을까?" = 반려 /
     "담보 단위로 합산하면 5,000만원이 보인다" = 본문에서 유지)
  - **"기존 계약이 잘못됐다" 프레이밍 금지** — 비방으로 읽힌다.
    각성의 근거는 **제도 변화와 담보 구조**에 둔다.
  - 개인의견 귀속 문구는 바이럴에서 **본문에 1회 이상** 필요하다.
    → **osmu-format이 자동 삽입한다**(「한 줄 조언」 블록 아래). 원고에 손으로 넣지 않는다.
  - **소재별 조건부 유의문구**(`brand.ts` `CONDITIONAL_NOTICES`) 해당 여부를 STEP 3에서 판정해
    본문에 넣는다. 실손·유병자·운전자·태아·해지환급 소재는 특히 확인.

**STEP 4 — 채널 원고 + 이미지 (Claude)**
- 본진 + 네이버 + 블로그스팟 3종 제목 분리(자기잠식 방지) + 각 채널 본문. 문체도 분리한다.
  - 본진: 정중·서술형 + 「신순주 지사장의 한 줄 조언」 인용블록 + FAQ
  - 네이버: 친근·구어체, `▶`/`·` 기호 정리, **`[이미지①②③]` 마커 삽입**
    ⚠️ **진단 URL을 원고에 손으로 넣지 않는다.** `/diagnosis` CTA는 osmu-format에서 기본 off이며
    (`includeDiagnosisCta`), 첫 심의 판정(§6.11) 이후 한 줄로 복원한다.
  - 블로그스팟: 개조식(번호·불릿 중심)·경어체, HTML(h2/ul), 본진 앵커 링크
    ⚠️ 개조식은 '구조'이지 '문체'가 아니다. 종결어미는 전 채널 경어체로 통일한다.
- **네이버 이미지 3장은 `naver_images.py` 고정 틀로 렌더한다** (Claude가 실행. 로버트는 PNG만 받음).
  맨 위 CONFIG의 텍스트만 교체 — 레이아웃·색·폰트는 건드리지 않는다(브랜드 일관성).
  ①대표 썸네일(딥그린) ②비교표(크림) ③핵심3+CTA밴드. 800×800.
- ⚠️ **이미지 컴플라이언스(§6.10)**: 대표이미지에 **금액 노출 금지**, 화폐 기호(₩, $)·지폐·동전 이미지 금지.
  `naver_images.py` / `insta_cards_v2.py` / `bignum.py` 출력물이 이 규칙에 걸리는지 **렌더 후 확인**한다.
- **네이버 원고에는 반드시 「발행 정보」 블록을 함께 준다:** 카테고리 · 제목 · 태그 · 이미지 배치 · 대표사진 지정.

**STEP 5 — 광고심의 10단계 (§6.9)** — 심의 없이 공개 게시하면 금소법 위반. 전 게시물 사전 심의 대상.

- **5-1 DB 투입** — Claude가 Supabase MCP로 `premium_articles`에 **초안(draft) INSERT**.
  (/admin에는 글쓰기 기능이 없어 MCP 직접 INSERT다)
  - `content_type='evergreen'`, `seed_key`=주제 키, `needs_human_review=true`, **발행 플래그 4종 모두 false**
  - 넣는 필드: title/slug/category/summary/tags · main_website_markdown · naver_blog_content · blogspot_content ·
    naver_title/blogspot_title · instagram_caption · key_points · faq_json · verify_claims · remodeling_bridge ·
    raw_source_fulltext(근거 교차확인 원문 — /admin 좌우 대조용)
  - faq_json 형식: `[{"question":…,"answer":…}]` / verify_claims: `[{"claim":…,"basis":…,"confidence":…}]`
  - ⚠️ MCP 직접 수정은 revalidatePath를 트리거하지 않아 ISR 캐시가 최대 60초 지연된다.
- **5-2 비공개 게시** — 3채널 모두 준비한다.
  - 웹사이트: `is_main_published=false` 유지. **별도 게시 행위 없음.**
  - 네이버/블로그스팟: /admin에서 **[심의용 복사](앰버 버튼)**로 원고를 받아 각 채널에
    **비공개(초안) 상태로 붙여넣는다.**
  - ⚠️ **[게시용 복사]가 아니다.** 심의용은 필수안내사항의 심의필 줄이 **공란(제_____호)**이고,
    게시용은 실번호가 들어간다. 승인 전에는 게시용 버튼이 비활성이다.
- **5-3 캡처 3종 — 이게 5-3 캡처 세트다.**
  - 웹사이트: /admin **[웹 심의용 미리보기]** → `/preview/news/{slug}?token=…`
    (미발행 초안이어도 열리고, 공개 페이지와 같은 화면이다. **워터마크 없음**)
  - 네이버 / 블로그스팟: 비공개 게시된 화면을 각각 캡처
  - 3종 모두 **비공개 상태가 화면에 보이도록** 캡처한다.
  - **페이지가 나뉜 하나의 PDF**로 만든다.
  - ⚠️ 캡처에 **필수안내사항이 반드시 포함**돼야 한다(누락은 반려 사유). 브라우저 인쇄로 PDF를
    뽑는 경우 색 반전 스타일이 적용되어 **'배경 그래픽' 체크 여부와 무관하게 읽힌다**
    (MandatoryNotice `@media print`).
  - ⚠️ 캡처 화면에 **브라우저 확장 배너·확대 팝업·개발도구**가 찍히지 않게 한다.
- **5-4 증빙 PDF 추가** — STEP 2 산출물(파일명 4요소 규칙).
- **5-5 zip 1개로 압축.**
- **5-6 팜스 신청** — 기타관리 → 광고심의 → [신청] → 심의유형 선택.
  ⚠️ **[저장]은 임시저장이다. [제출]을 눌러야 접수된다.**
  게시명에 특수문자(`' ? " &`) 금지. 실제 글 제목과 달라도 무방.
- **5-7 영업일 1~2일 대기.**
- **5-8 심의필 번호·유효기간 수령 → `ad_reviews`에 채널별 행 입력**
  (`status='approved'`, `review_no`, `review_from`, `review_to`).
- **5-9 공개 전환** — /admin에서 발행. ⚠️ `trg_publish_gate`가 심의필 없으면 **DB 레벨에서 막는다.**
  채널별 복사 버튼이 **필수안내사항을 자동으로 붙인다**(osmu-format).
  - ⚠️ 네이버·블로그스팟은 승인 후 **[게시용 복사]로 원고를 다시 받아 교체 게시**한다.
    비공개로 올려둔 심의용 원고(심의필 공란)를 **그대로 공개 전환하면 안 된다**(공란 = 허위 표기).
- **5-10 팜스에 실제 게시 URL 등록** — **누락 시 이후 신규·연장 심의가 제한된다.**
  `ad_reviews.posted_url` / `url_registered_at`도 함께 채운다.

> ⚠️ **심의 통과 후 원안 수정 불가**(오타 포함). 변경하려면 연장이 아니라 **신규 재신청**이다.
> → 검수는 **5-3 캡처 이전에** 끝내야 한다. 캡처 후 발견한 오타는 재신청 비용이다.

**STEP 6 — 인스타 (선택 · 주 3~4회 리듬)**
- 인스타는 **매 글마다 올리지 않는다.** 표·기준·숫자가 있는 글만 카드로 만든다(서사형은 캡션/릴스).
- 카드는 `insta_cards_v2.py`(확정 틀)로 렌더. 1080×1350 · 7장. CONTENT와 각 카드 문구만 교체.
- 캡션은 STEP 4에서 생성한 `instagram_caption` 사용(/admin 복사 버튼).

**STEP 7 — 스레드 (계정 세팅 후)**
- 스레드는 텍스트 전용이라 부담이 낮고 전문가 톤이 그대로 먹힌다. 글 1편에서 3~4개가 나온다.
- 소재: 「한 줄 조언」 / 놀라운 사실 하나 / 오해 교정 / 숫자 하나.
- ⚠️ **심의 유형이 별도다.** 스레드는 **텍스트 전용이면 팜스 폼에 직접 입력**(캡처·PDF·zip 불필요),
  **이미지가 포함되면 일반심의**로 신청한다.
- ⚠️ 반송 시 스레드는 [보완]이 아니라 **신규 재신청**이다(블로그·유튜브만 [보완] 가능).
- ⚠️ **[미확정]** 사전등록 플랫폼 선택지에 스레드가 없다. **처리 방법 확인 전까지 스레드 게시 0건 유지.**
- 그 전까지는 Claude가 채팅에서 스레드 문구를 뽑아 로버트가 직접 게시(심의 통과 후).

**STEP 8 — 기록**
- TOPIC-BANK.md 해당 주제 상태를 ✅로, 본진 URL과 발행 채널 기록.
- DB 발행 플래그(is_main/naver/blogspot/instagram_published) 4종 확인.
- **`ad_reviews` 행의 `posted_url`·`url_registered_at` 기입 확인**(5-10 누락 방지).
- **만료일(`review_to`)을 캘린더에 등록** — 유효기간 관리(§6.3)는 사람 기억에 맡기지 않는다.

---

## 인스타 카드 디자인 시스템 (v2 · 2026-07-18 확정)

**확정 리듬(A안):** 오버라인 → 제목(상단 고정) → 구분선 → 수치·근거 → 알약 → 출처 → 브랜드 푸터
- 제목을 위에 두는 이유: 피드 격자 썸네일에서 아래쪽이 잘려 무슨 글인지 안 읽힌다. 또 "정확히 알려주는" 브랜드 톤과 순서가 맞다.
- 빈 공간을 남기지 않는다. 사진이 없는 대신 타이포·알약·출처로 프레임 아래까지 채운다.

**시각 처리**
- 배경: 딥그린 그라디언트(#20463a→#0f2419) / 크림 그라디언트(#fdfcfa→#f1ece1). 카드마다 교차 배치해 넘길 때 리듬을 만든다.
- 미세 노이즈 5% — 평면 디지털 느낌 제거.
- **카드 전용 밝은 골드 `#e3b23c`** — 어두운 배경에서만 사용. 기존 골드(#a8842c)는 어두운 배경에서 탁해 죽는다.
  밝은 배경(크림)에서는 기존 골드를 그대로 쓴다. ⚠️ **웹·네이버 이미지에는 밝은 골드를 쓰지 않는다**(채널별 규칙 분리).
- 채운 알약 배지(저장 필수 등) / 표 행·강조 박스에 그림자.

**커버 변주** — 피드 격자에는 1번 카드만 보인다. 연속 게시물은 서로 다른 타입으로:
A 숫자형(딥그린) · B 질문형(크림) · C 대비형(분할) · D 목록형(딥잉크). A→B→C→D 순환.
내부 카드(2~7)는 일관성 유지 — 거긴 브랜드 자산이다.

**⚠️ 거대 숫자 안전장치 (두 번 재발한 버그)**
- 폰트 폭만 계산하고 높이를 안 보면 쉼표·받침 꼬리가 아랫줄을 파고든다. A·B 두 레이아웃에서 연달아 발생.
- `draw_bignum()`이 bbox 하단(꼬리 포함)을 실측해 **다음 요소의 안전 y를 반환**한다. 폰트 크기를 바꿔도 안 겹친다.
- 단위(원/회)는 쉼표가 아니라 **숫자 몸통 기준**으로 정렬한다.
- 교훈: 발견된 시각 버그는 그때그때 좌표를 옮겨 막지 말고 **틀에 안전장치로 넣는다.**

---

## 검수 책임 분담 (⚠️ 1호에서 확정)

| 항목 | 누가 |
|---|---|
| 수치·법령·시행일 사실 확인 | **Claude** (교차확인, 애매하면 삭제 또는 조건부 서술) |
| 제도·상품의 출시/시행 여부 | **Claude** ("예정"인지 "이미 시행"인지 반드시 확인) |
| 컴플라이언스(비방·보장·과장·무조건해지) | **Claude** |
| 우리 고객에게 유용한 글인가 | **로버트** |
| 신순주가 할 만한 말인가(톤·관점) | **로버트** |
| 컴플라이언스 1차 판정(금지어·금액 노출·조건부 유의문구 누락) | **Claude** |
| 심의 신청·제출·게시 URL 등록 | **로버트** (팜스 계정 접근이 사람만 가능) |

- 로버트에게 "이 수치 맞는지 확인해주세요"라고 넘기지 않는다. 판단할 수 없는 것을 검수로 요구하는 것은 검수가 아니다.
- **증빙 PDF 확보 실패는 Claude가 STEP 2에서 보고한다.** 원고를 다 쓰고 나서 발견하면 재작성이다.

---

## 다음 채팅에서 이어가는 법 (맥락 유지)

- 프로젝트 지식에 **TOPIC-BANK.md + 이 문서 + CLAUDE.md §6(3차 개정본)/CONTENT-STRATEGY.md/PROGRESS.md**가 있으면, 새 채팅에서 "건강보험 상록수 쓰자"라고만 해도 Claude가 이 절차대로 이어간다.
- ⚠️ **CLAUDE.md §6.5(유인 요소 분리 → 연 1회 심의)는 폐기됐다.** 모든 게시물이 게시 전 사전 심의 대상이다. §6.5를 근거로 판단하지 않는다.
- 로버트는 새 채팅에서 TOPIC-BANK.md의 상태만 보면 "뭘 썼고 뭐가 남았는지" 바로 안다.

## 남은 로드맵 (이 트랙 밖 — 안 잊기용)

- 네이버 데이터랩 API로 주제·연관키워드 확장([1] 강화) — 주제가 부족해질 때
- 완전 자동(A) 검토 — 검수 캐파·발행량 안정 후
- 인스타 포맷 다양화(케이스 카드·릴스) + 클로드 디자인 프롬프트
- 네이버 이미지 결정적 렌더(제목·key_points 템플릿, AI 불필요)
- voice-dna(신순주 목소리 학습) — 사례집 답변 수령 후
