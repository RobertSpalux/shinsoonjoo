# DESIGN-SPEC — 신순주의 선한 금융

> CLAUDE.md 4절(라이트/에디토리얼)의 실행 명세. 컴포넌트·카피 작업 시 이 문서의 토큰과 규칙을 따른다.
> 상태: **확정 · globals.css 반영됨** (2026-07-09) — 텍스트 색은 B안(웜 차콜) 채택.
> 갱신: 딥 그린 대비 밴드(3-4) 추가 + 주 CTA·섹션 헤딩 잉크를 딥 파인(`--color-forest`)으로 전환(1-3·4-1).

---

## 1. 컬러 토큰

### 1-1. 배경·보더 (현행 유지)

| 토큰 | hex | 용도 |
|---|---|---|
| `--color-ink` | `#faf9f6` | 페이지 기본 배경 (웜 아이보리) |
| `--color-ink-soft` | `#f3efe7` | 교차 섹션 배경 |
| `--color-ink-card` | `#ffffff` | 카드·입력 필드 배경 |
| `--color-line` | `#e8e2d6` | 기본 보더·구분선 (1px) |

### 1-2. 텍스트 — 두 안 비교

| | (A) 현행 쿨 슬레이트 | (B) 웜 차콜 ✅ **채택** |
|---|---|---|
| 헤딩 | `#0f172a` / `#1e293b` | `#221e18` |
| 본문 | `#334155` | `#48423a` |
| 캡션·보조 | `#64748b` | `#6b6457` |
| 장점 | 이미 적용돼 있어 작업량 0, 익숙한 고대비 | 배경(웜 아이보리)과 색온도가 일치해 지면 전체가 한 장의 종이처럼 읽힘 — 에디토리얼의 전제 |
| 단점 | 웜 배경 위에서 푸른 기가 떠 보임(온도 충돌), Tailwind 기본 팔레트라 "템플릿 티" | 기존 하드코딩 slate 값 일괄 치환 필요 |

**권장 근거 (B):** 종이 잡지는 잉크와 지면의 색온도를 맞춘다. 웜 아이보리(황색 기) 배경 위의 쿨 슬레이트(청색 기)는 미세한 부조화를 만들고, 특히 `#1e293b`는 Tailwind `slate-800` 기본값이라 CLAUDE.md가 금지한 "AI 템플릿 느낌"의 대표 신호다. 웜 차콜은 대비를 유지하면서(모두 WCAG AA 통과: `#221e18` 대비 ≈ 15.9:1, `#48423a` ≈ 9.2:1, `#6b6457` ≈ 5.3:1) 지면 온도를 통일한다.

신규 텍스트 토큰 4개를 **추가**한다 (기존 변수명은 전부 유지 — 컴포넌트 호환):

```css
--color-text-strong: #221e18;       /* 헤딩·주CTA 배경 = '확신에 찬 액센트' */
--color-text-strong-hover: #3a342b; /* 주CTA hover 배경 (text-strong의 한 단계 밝은 잉크) */
--color-text-body:   #48423a;       /* 본문. 이보다 연한 본문 금지 (가독성) */
--color-text-muted:  #6b6457;       /* 캡션·라벨·메타 전용. 본문에 쓰지 않는다 */
```

기존 하드코딩 slate 값 치환 맵 (globals.css 반영 단계에서 적용):
`#0f172a`,`#1e293b` → `--color-text-strong` / `#334155` → `--color-text-body` / `#64748b` → `--color-text-muted` / 스크롤바 `#cbd5e1` → `#d6d0c4`

### 1-3. 액센트

**액센트는 하나 = 딥 파인(`--color-forest`).** 섹션 헤딩과 주 CTA 버튼 배경이 이 색이다 (2026-07 딥 잉크에서 전환 — 3-4 대비 밴드와 같은 계열로 색 깊이 부여). `--color-text-strong`은 본문 강조·카드 제목·소형 UI의 잉크로 유지. 골드는 액센트가 아니라 "속삭임"이다.

`--color-forest`는 밴드 배경 외에, **'결론값' 잉크**로도 사용한다 (2026-07-13 확장 — 커밋 O5).
허용: 목업 신규값(after), 진단 점수 카드의 핵심 위험 항목.
금지: 본문·캡션·일반 UI 텍스트. 여전히 남용 금지 — 화면당 몇 개의 '결론'에만.

| 토큰 | hex | 용도 (⚠️ 주석으로 강제) |
|---|---|---|
| `--color-gold` | `#a8842c` | **얇은 선(1px 헤어라인)·작은 마크·언더라인 전용.** 버튼 채움, 헤딩 단어색, 넓은 배경, 숫자색 등 면(面) 사용 금지 |
| `--color-gold-light` | `#c09b3a` | 골드 선·마크의 hover 상태 전용 |
| `--color-gold-dim` | `#d9c893` | 은은한 보더·언더라인 등 저채도 포인트 |

globals.css 반영 시 토큰 옆 주석:
```css
--color-gold: #a8842c;      /* ⚠️ 얇은 선·작은 마크 전용. 면 채움(버튼·배경·헤딩 단어색) 금지 */
```

`.article-body strong`(현재 골드 텍스트 = 면적 사용)은 `--color-text-strong` + weight 700로 변경한다.

---

## 2. 타이포그래피 스케일

- **헤드라인 = `Noto Serif KR`** (`--font-serif`) / **본문·UI = `Pretendard`** (`--font-sans`)
- 모바일 퍼스트: `크기`는 `모바일 → md 이상` 순.

| 레벨 | 폰트 | size | weight | line-height | letter-spacing |
|---|---|---|---|---|---|
| h1 (히어로) | Serif | 2.25rem → 3.25rem | 600 | 1.22 | -0.015em |
| h2 (섹션 헤드) | Serif | 1.75rem → 2.25rem | 600 | 1.3 | -0.01em |
| h3 (카드·서브섹션) | Serif | 1.25rem → 1.5rem | 600 | 1.4 | -0.01em |
| h4 (UI 소제목) | Sans | 1.0625rem → 1.125rem | 600 | 1.45 | -0.01em |
| body | Sans | 1rem (기사 본문 1.0625rem) | 400 | 1.75 (기사 1.85) | -0.01em |
| caption·라벨 | Sans | 0.8125rem | 500 | 1.5 | 0 |
| 오버라인 라벨* | Sans | 0.75rem | 600 | 1.4 | 0.08em (대문자/자간 라벨은 이 한 종만) |

- \* 오버라인 라벨: 섹션 헤더 상단의 작은 분류 라벨 1종에만 허용. 그 외 대문자 트래킹 금지 (CLAUDE.md 4절).
- 예외: **기사 본문(`.article-body`)의 h3는 sans 허용** — h2 세리프와의 대비로 본문 내 위계를 만든다.
- 세리프 헤드라인 weight는 600~700까지만. `font-extrabold`(800) 금지 — 산세리프 대문짝 느낌의 잔재.
- **숫자 규칙:** 경력·통계·금액 등 모든 수치는 `font-variant-numeric: tabular-nums` (Tailwind `tabular-nums`) 적용. 카운트업 애니메이션 숫자는 필수(자리 흔들림 방지).

---

## 3. 간격 · 반경 · 그림자

### 3-1. 간격

| 용도 | 값 |
|---|---|
| 섹션 수직 패딩 | `5rem` → md `7rem` (`py-20 md:py-28`) |
| 섹션 헤더 ↔ 본문 | `3rem` (`mb-12`) |
| 카드 내부 패딩 | `1.75rem` → md `2rem` (`p-7 md:p-8`) |
| 카드 그리드 gap | `1.5rem` (`gap-6`) |
| 컨테이너 | `max-w-6xl` + `px-5 md:px-8`, 기사 본문은 `max-w-2xl` |

여백은 깎지 않는다 — 밀도가 높아 보이면 요소를 줄이지, 간격을 줄이지 않는다.

### 3-2. 모서리 반경 (2단계 위계 + 예외)

| 토큰 | 값 | 용도 |
|---|---|---|
| `--radius-lg` | `16px` | 카드, 이미지 프레임, 모달 등 큰 면 |
| `--radius-sm` | `8px` | 버튼, 입력 필드, 작은 태그 |
| 없음 (0) | `0` | 구분선 위 콘텐츠 블록, 인용 프레임 — 각을 살려 위계 대비 |

- 모든 요소 동일 반경 금지 (CLAUDE.md 4절). `rounded-full` 알약 형태는 UI 배지에 금지 (아바타 원형만 예외).

### 3-3. 그림자 (최소주의 — 기본은 1px 보더)

| 토큰 | 값 | 용도 |
|---|---|---|
| (기본) | 그림자 없음 + `1px solid var(--color-line)` | 카드·프레임의 기본 상태 |
| `--shadow-card` | `0 2px 12px rgba(34, 30, 24, 0.06)` | 부유가 필요한 카드 (폼, 목업 프레임) |
| `--shadow-lift` | `0 10px 30px rgba(34, 30, 24, 0.10)` | hover 상승 등 일시 상태 |

그림자 색은 검정이 아니라 잉크색(`34,30,24`) 기반 — 웜 톤 유지.

---

### 3-4. 대비 밴드 (Contrast Band) — 딥 그린

| 토큰 | hex | 용도 |
|---|---|---|
| `--color-forest` | `#1b3a30` | 딥 파인 — 대비 밴드 배경·주CTA·헤딩 잉크 |
| `--color-forest-soft` | `#24493d` | hover·서브 밴드 |

- 딥그린은 **'밴드'로만** 사용한다 — 푸터·통계 스트립·진단 섹션 등 지정 구역 한정. **전체 도배 금지.** 크림(웜 아이보리) ↔ 딥그린이 교차하는 리듬이 목적이다.
- **딥그린 밴드 위 규칙:**
  - 텍스트 = `--color-ink`(크림)
  - 헤딩 = 세리프 + 크림
  - 구분선·마크 = 골드 헤어라인(`--color-gold`)
  - 보조 텍스트 = 크림 70% 불투명 (`color-mix` 또는 `/70`)
- 골드는 딥그린 위에서 특히 **헤어라인·작은 마크로만** — 면 채움 금지 원칙 그대로 유지.

## 4. 컴포넌트 규칙

### 4-0. 공통 — 키보드 포커스 (focus-visible)

버튼·텍스트 링크·입력 필드 등 모든 인터랙티브 요소는 키보드 포커스 시 가시적 링을 갖는다.
hover 스타일과 **별개의 상태**이며, 마우스 클릭에는 나타나지 않도록 `:focus`가 아닌 `:focus-visible`을 쓴다.

```css
outline: 2px solid var(--color-gold);
outline-offset: 2px;
```

- globals.css의 전역 규칙(`:where(a, button, input, textarea, select, [tabindex]):focus-visible`)이 기본 적용된다. 컴포넌트에서 `outline-none`으로 제거하지 않는다.
- 골드 아웃라인은 "작은 마크" 허용 용도에 해당 (일시적 2px 링 — 면 채움 아님).

### 4-1. 주 CTA 버튼 (화면당 1개)

- 배경 `--color-forest`(딥 파인) / 텍스트 `--color-ink`(아이보리) / 반경 `--radius-sm` / 패딩 `px-7 py-3.5`
- hover: 배경 `--color-forest-soft` + `translateY(-1px)`, transition은 `transform`/`opacity`/`background`만
- focus-visible: 4-0 공통 규칙(골드 2px 링)
- **골드 채움 금지. 채움+외곽선 버튼 2개 나란히 금지** — 보조 액션은 4-2 텍스트 링크로.
- 문구는 방법론 CTA로 통일: "내 보험 진단받기" 계열.

### 4-2. 보조 텍스트 링크

- `--color-text-strong` + `border-bottom: 1px solid var(--color-gold-dim)` (골드의 허용 용도: 얇은 선)
- hover: 보더 `--color-gold`, 텍스트 유지. 필요 시 꼬리표 `→`는 텍스트로만 (알약 배지 금지).
- focus-visible: 4-0 공통 규칙(골드 2px 링)

### 4-3. 카드

- 배경 `--color-ink-card` / 보더 `1px solid var(--color-line)` / 반경 `--radius-lg` / 그림자 기본 없음
- hover가 필요한 인터랙티브 카드만 `--shadow-lift` + `translateY(-2px)`
- 카드 안 제목은 h3(Serif), 메타는 caption(`--color-text-muted`)

### 4-4. 섹션 헤더

```
[오버라인 라벨]  ← 0.75rem / 600 / tracking 0.08em / --color-text-muted
[세리프 h2]      ← 아래 골드 헤어라인 24px × 1px (--color-gold) — 골드의 허용 용도
[한 줄 설명]     ← body / --color-text-muted, 선택적
```

- 정렬은 섹션 성격에 따라 좌측 기본, 히어로급만 중앙. 전 섹션 중앙정렬 반복 금지.

### 4-5. 구분선

- 기본: `1px solid var(--color-line)` 풀폭
- 강조(페이지당 1곳 이내): `24~40px × 1px var(--color-gold)` 짧은 헤어라인 — 골드 허용 용도
- 그라디언트 구분선, 두꺼운 골드 라인 금지

---

## 5. 반영 순서

1. ✅ globals.css: 텍스트 토큰 4종 추가 + slate 하드코딩 치환 + 골드 주석 + radius/shadow 토큰 추가 + focus-visible 전역 규칙 + `.article-body strong` 색 변경 (반영됨)
2. 이후 STEP 3(히어로)부터 컴포넌트별 적용
